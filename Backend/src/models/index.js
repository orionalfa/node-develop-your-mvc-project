const { Products } = require("./product-model");
const { Models } = require("./model-model");
const { Users } = require("./user-model");

module.exports = {
  Products: Products,
  Models: Models,
  Users: Users,
};
